@page
@model NoTrick.Web.Pages.Admin.Supplier.Index

@{
    ViewData["Title"] = "Home page";
}
<!-- Content Header (Page header) -->
<div class="content-header">
  <div class="container-fluid">
    <div class="row mb-2">
      <div class="col-sm-6">
        <h1 class="m-0 text-dark">Suppliers</h1>
      </div><!-- /.col -->
      <div class="col-sm-6">
        <ol class="breadcrumb float-sm-right">
          <li class="breadcrumb-item"><a asp-page="/Admin/Supplier/Index">Suppliers</a></li>
          <li class="breadcrumb-item active">Suppliers</li>
        </ol>
      </div><!-- /.col -->
    </div><!-- /.row -->
  </div><!-- /.container-fluid -->
</div>
<!-- /.content-header -->

<!-- Main content -->
<div class="content">
  <div class="container-fluid">
    <div class="card">
      <div class="card-body">
        <div class="d-flex flex-row flex-wrap">
          <a class="btn btn-outline-success m-1" asp-page="Edit"><i class="fas fa-plus"></i></a>
        </div>
        <table class="table table-borderless">
          <tr>
            <th>Supplier Id</th>
            <th>Company Name</th>
            <th>Balance</th>
            <th>Actions</th>
          </tr>
          @foreach (var supplier in Model.Suppliers) {
            <tr>
              <td class="AccountId">
                <span>@supplier.Id</span>
              </td>
              <td class="EMail">
                <span>@supplier.CompanyName</span>
              </td>
              <td class="CreatedAt">
                <span>@supplier.Balance</span>
              </td>
              <td>
                <div class="d-flex flex-row flex-wrap">
                  <a class="btn btn-outline-success m-1" asp-page="Edit" asp-route-id="@supplier.Id">Edit</a>
                  <a class="btn btn-outline-danger m-1" asp-page="Index" asp-page-handler="Remove" asp-route-id="@supplier.Id">Remove</a>
                  <a class="btn btn-outline-info m-1" asp-page="Managers" asp-route-id="@supplier.Id">Managers</a>
                </div>
              </td>
            </tr>
          }
        </table>
      </div>
    </div>
  </div>
</div>
  
<!-- /.main-content -->
